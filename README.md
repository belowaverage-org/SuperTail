
# SuperTail

SuperTail is a fast, lightweight, and polished **multi-file tail** application for Windows. It discovers log files across directory trees and **tails them simultaneously**—in one unified, high-performance view.

---

## 🚀 Features

- **Automatic Discovery**
  - Point SuperTail at any local or UNC path; it recursively discovers logs that match your filter.
- **Live Multi-File Tail**
  - Tails dozens (or hundreds) of files at once using efficient watchers and buffered incremental reads.
- **Smart Filtering**
  - Flexible **regex filter** (default matches `.txt` and `.log`) to include/exclude files.
- **Custom Log Viewer**
  - Compact grid layout.
  - Smooth horizontal/vertical scrolling, double-buffered drawing, and virtualized rendering.
  - Optional per-file color rules and quick source toggling.
- **Responsive UI**
  - Minimal overhead: status bar shows **Selected**, **CPU%**, and **Memory** usage.
- **Zero-Config Defaults**
  - Sensible defaults for sharing flags, retries, and large-file handling.
